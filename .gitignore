# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv*/
.env

# Testing
.pytest_cache/
.coverage
htmlcov/

# OS
*.DS_Store

# Internal dev context (not for contestants)
CLAUDE.md

# Reference rules from sibling competitions (kept locally for drafting)
rules/reference/

# Claude Code internal state
.claude/

# Overnight session handoff notes (local only)
OVERNIGHT_NOTES.md

# External dependencies (cloned separately)
axolver/

# Model caches (HF downloads)
.cache/

# Leaderboard state
leaderboard.json

# Model weights and checkpoints (all models live on HuggingFace)
*.pt
*.pth
*.bin
*.safetensors
*.gguf
*.onnx

# Exception: the three compliant reference models ship their weights in-repo
# so the examples are clone-and-run without a separate download.
!examples/digit_transformer/weights.pt
!examples/dlp_grokking/weights.pt

# Local example exploration (not for distribution until approved)
examples/exploration/
